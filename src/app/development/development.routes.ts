import { Route } from '@angular/router';
import { DevelopmentComponent } from './index';

export const DevelopmentRoutes: Route[] = [
  {
    path: 'development',
    component: DevelopmentComponent
  }
];